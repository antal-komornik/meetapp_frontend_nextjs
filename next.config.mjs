// /** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'via.placeholder.com',
      'api.komornikantal.hu',
      'img.daisyui.com'
    ], // Itt add meg az engedélyezett domain neveket
  },
}

export default nextConfig

